{
  "name": "tabtracker-backend",
  "version": "1.0.0",
  "description": "Connection to the sql DB using express and node",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"./node_modules/nodemon/bin/nodemon.js src/app.js --exec 'npm run lint && node'",
    "lint" : "./node_modules/.bin/eslint **/*.js"
  },
  "author": "Tonny",
  "license": "ISC",
  "dependencies": {
    "eslint": "^8.2.0",
    "nodemon": "^2.0.14",
    "save": "^2.4.0"
  }
}
